generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model dragon_warrior {
  id   Int    @id @default(autoincrement())
  name String
}
