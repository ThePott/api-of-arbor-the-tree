{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
        "builder": "RAILPACK",
        "buildCommand": "bash ./railway-build.sh"
    },
    "deploy": {
        "startCommand": "export PATH=$PATH:/app/bin && npm run start"
    }
}
