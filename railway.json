{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
        "builder": "RAILPACK",
        "buildCommand": "bash ./railway-build.sh"
    },
    "deploy": {
        "startCommand": "export PATH=$PATH:/app/bin  && export TYPST_FONT_PATHS=/app/dist/src/features/assignment/pdf/fonts && npm run start"
    }
}
